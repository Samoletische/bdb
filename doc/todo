- сейчас, если база данных не готова (не можем подключиться), то и бот не создаётся, и сообщение нельзя принять, и отправить ответ нельзя.
  - Сделать так, чтобы бот создавался, а при создании ответа нельзя было только сформировать ответы, которые зависят от данных в БД.
- вставить проверки настроек пользователя (есть ли он в базе, подключен ли к кошельку) при создании пользователя. Сейчас это закомментировано в makeAnswer.
- заменить параметры функций на константы
- сделать упор на то, что, если даже у нас возникают исключительные ситуации, ошибки или приходят некорректные данные, чтобы пыталась ошибка записаться в лог и, обязательно, отправилось сообщение пользователю о не понимании.
- в createStorage сделать не вызов исключения, а возврат NULL. Это к предыдущему замечанию
- переместить список известных команд (command_) в настройки
+ ввести роли. Пересмотреть команды бота и др классов на предмет зависимости от ролей.
+ добавить в Bot:query отлов исключения StorageException?
+ перепроверить в классе Bot корректность написания функции query (исключения, значения для возврата)
- перепроверить вызовы исключений (например в command_entries)
- для первого релиза необходимо сделать:
  + проверку на наличие роли админа при выполнении команды подключения пользователя.
  + добавить в команды (только для админа) подкоманду "дата" с указанием даты операции
  - доделать Commands::getBalance
  - добавить команду "Операции" (с группировкой по времени, статье, комменту)
  - после запуска релиза актуализировать документацию
+ setUserActivity возвращает ошибку, хотя выполняет
- command_balance исправить получение последнего дня. нужно получать день того месяца, а не текущего
+ в показе вывести время и общий итог
+ проверить в других командах вывод даты без времени (занимает много места)
- у функций класса Bot добавить параметр queryStrOnly, по значению которого мы получаем не результат, а только строку запроса. Это нужно для присоединения к этому запросу другого и выполнение пакетного запроса.
- реализовать в классе бот функцию возврата последней ошибки хранилища
- добавить функцию array Message::addToContent(String $str) для добавления строки в имеющийся контент сообщения
- некорректно срабатывает определение пользователя по имени

+ создание бота:
	+ создал приватную группу в ВК - ФрутоРадуга (club184068927, group_id=184068927)
	+ в управлении API создал ключ - 4c1676e87257d06b8be701735285527381d83f5a0079f965b8cd81f78af74b50c5044d58c949dc6900adf
	+ указываем адрес бота - http://cr97917.tmweb.ru/frura_bot/bot.php (confirmation=94d549c5)
	+ создаём standalone-приложение на https://vk.com/dev (id=7043469)
	+ включаем "Работа с API". Указываем адрес http
	+ для получения токена переходим по ссылке https://oauth.vk.com/authorize?client_id=7043469&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=friends,photos,audio,video,status,messages,wall,groups,offline,stats,email&response_type=token&v=5.63
	(прим. messages с запятой убираем)
	+ получаем ответный токен (строка полностью) - https://oauth.vk.com/blank.html#access_token=c1c54ca3eec1c6d97871e828b43d9fbc4be3ae939f0012af589382cb18f2037c3eeb77a898be18e8ff42c&expires_in=0&user_id=432201510
	а точнее сам токен (access_token) - c1c54ca3eec1c6d97871e828b43d9fbc4be3ae939f0012af589382cb18f2037c3eeb77a898be18e8ff42c
  8c785675048b79d4770a1c5275b4986efeddfa31b96a2d33110d2e8025c9fddb7b3393541c6afdf6022b9
  + включаем в сообществе входящие сообщения на вкладке "Типы событий"
